import { RulesListSearchPipe } from './rules-list-search.pipe';

describe('RulesListSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RulesListSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
